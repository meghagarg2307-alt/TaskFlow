# =============================================================================
# This file is no longer the runtime config.
#
# Runtime config is generated at container start from:
#   infra/nginx.conf.template
# via the official nginx image envsubst entrypoint (see frontend/Dockerfile).
#
# Local Compose defaults (set on the `web` service):
#   API_UPSTREAM=http://api:8080
#   API_HOST=api
#   NGINX_RESOLVER=127.0.0.11
#
# Render / external API example:
#   API_UPSTREAM=https://taskblow-backend.onrender.com
#   API_HOST=taskblow-backend.onrender.com
#   NGINX_RESOLVER=1.1.1.1
# =============================================================================
